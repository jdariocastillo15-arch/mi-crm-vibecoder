# Dependencias
node_modules/
.pnp
.pnp.*

# Build de Next
.next/
out/
build/
next-env.d.ts

# Entorno — nunca subir claves
.env
.env.local
.env*.local

# El token con el que Sentry sube los source maps, si alguna herramienta suya
# llega a escribirlo aquí
.env.sentry-build-plugin

# Convex
.convex/

# Sistema
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel

# Carpeta de trabajo temporal
TMP (TEMPORAL)/

# TypeScript
*.tsbuildinfo

# Vale de autorización de push — nunca se versiona
.claude/push-autorizado

# Playwright — la sesión guardada lleva tokens y nunca se sube
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/e2e/.auth/
